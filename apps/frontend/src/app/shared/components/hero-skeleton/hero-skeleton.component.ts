/**
 * Placeholder skeleton component for the hero section
 *
 * @description Stub component to prevent build errors.
 * TODO: Replace with actual hero skeleton implementation.
 */
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-hero-skeleton',
  standalone: true,
  imports: [CommonModule],
  template: `<div class="hero-skeleton"></div>`,
})
export class HeroSkeletonComponent {}
