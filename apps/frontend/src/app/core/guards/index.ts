/**
 * Core Guards Barrel Export
 *
 * @description
 * Centralizes exports for all application guards.
 * Provides clean import paths for route protection guards.
 *
 * @example
 * ```typescript
 * import { permissionGuard } from '@app/core/guards';
 * ```
 */

export * from './permission.guard';
